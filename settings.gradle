pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://maven.lokalise.com"
        }
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://maven.lokalise.com"
        }
    }
}
rootProject.name = "LokaliseDemo"
include ':app'
